buildscript {
  repositories {
    jcenter()
  }
  dependencies {
    classpath 'org.ajoberstar:gradle-git:1.4.2'
  }
}

version = '0.0.2'

apply plugin: 'java'
apply plugin: 'maven'

apply from: 'gradle/readme.gradle'
